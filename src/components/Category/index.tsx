import React from 'react'

import { ICCategory } from '../../types'
import Interface from './interface'

const Category: ICCategory = props => {
    return <Interface {...props} />
}

export default Category
