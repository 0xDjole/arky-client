import React from 'react'

import { ICDrawer } from '../../../types'
import Interface from './interface'

const Drawer: ICDrawer = props => {
    return <Interface {...props} />
}

export default Drawer
