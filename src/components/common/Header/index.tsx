import React from 'react'

import { ICHeader } from '../../../types'
import Interface from './interface'

const Header: ICHeader = props => {
    return <Interface {...props} />
}

export default Header
